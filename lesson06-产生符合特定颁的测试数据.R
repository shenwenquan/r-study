#二项分布
str(rbinom)


#随机分布
# 0-1 分布
(x = rbinom(5, 1, 0.7))
# x~(N,P)
(x = rbinom(5, 100, 0.7))


#概率密度
# 做100次实验，发生0.7概率的事件次数为20的概率是多少？
(y = dbinom(20,100,0.7))
(y= dbinom(60:90,100,0.7))

#累积概率1
sum(y)

y = dbinom(0:100,100,0.7)
#概率密度曲线：
plot(0:100,y,pch=16)



#累积概率2
y = pbinom(20,100,0.7)

pbinom(90,100,0.7)-pbinom(60,100,0.7)

plot(0:100,pbinom(0:100,100,0.7),pch =16)


#得到分位数
x = qbinom(0.4,100,0.7)
x

str(rnorm)
